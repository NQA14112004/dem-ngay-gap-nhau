# Thư mục dựng bản deploy, không cần theo dõi
dist/

# Rác của hệ điều hành và trình soạn thảo
Thumbs.db
desktop.ini
.DS_Store
*.swp

# Chỉ đẩy lên đúng file nhạc đã cắt gọn. File gốc 137 MB và mọi file nhạc khác
# đều bỏ qua - GitHub từ chối file trên 100 MB, và người xem cũng không nên
# phải tải cả trăm MB chỉ để nghe nhạc nền.
assets/audio/*.mp3
assets/audio/*.m4a
!assets/audio/nhac-nen.mp3

node_modules/
